syntax = "proto3";

package com.duan.session;

enum UserSessionStatus {
    DONT_USE_0_IN_USERSESSIONSTATUS = 0;
    OPEN = 1;
    CLOSED = 2;
    PENDING = 3;
}

message UserSession {
    int64 id = 4;
    UserSessionStatus status = 1;
    string user_id = 2;
    string session_id = 3;
}
